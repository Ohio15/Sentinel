name: CI

on:
  push:
    branches: [master, main]
  pull_request:
    branches: [master, main]

jobs:
  # Go Agent - Build and Test
  agent:
    name: Agent Build & Test
    runs-on: windows-latest
    defaults:
      run:
        working-directory: agent

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.21'
          cache-dependency-path: agent/go.sum

      - name: Verify dependencies
        run: go mod verify

      - name: Run go vet
        run: go vet ./...
        continue-on-error: true  # Windows syscall code triggers unsafe.Pointer warnings

      - name: Run tests
        run: go test -race -timeout 60s ./...
        continue-on-error: true  # Some tests require Windows-specific environment

      - name: Build agent
        run: go build -o sentinel-agent.exe ./cmd/sentinel-agent

      - name: Build watchdog
        run: go build -o sentinel-watchdog.exe ./cmd/sentinel-watchdog

  # Web Frontend - Build and Lint
  app:
    name: Web App Build & Lint
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run ESLint
        run: npm run lint
        continue-on-error: true

      - name: TypeScript type check
        run: npx tsc --noEmit

      - name: Build web frontend
        run: npm run build:web

  # Cross-platform agent builds (on release tags)
  agent-release:
    name: Agent Release Builds
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    needs: [agent, app]
    defaults:
      run:
        working-directory: agent

    strategy:
      matrix:
        include:
          - goos: windows
            goarch: amd64
            ext: .exe
          - goos: linux
            goarch: amd64
            ext: ''
          - goos: darwin
            goarch: amd64
            ext: ''
          - goos: darwin
            goarch: arm64
            ext: ''

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.21'

      - name: Build agent
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
        run: |
          go build -o sentinel-agent-${{ matrix.goos }}-${{ matrix.goarch }}${{ matrix.ext }} ./cmd/sentinel-agent

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: agent-${{ matrix.goos }}-${{ matrix.goarch }}
          path: agent/sentinel-agent-${{ matrix.goos }}-${{ matrix.goarch }}${{ matrix.ext }}
