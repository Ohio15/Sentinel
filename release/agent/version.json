{
  "version": "1.77.39",
  "releaseDate": "2026-06-19",
  "changelog": "Build + deploy infrastructure cycle (no functional agent changes vs 1.77.38). Unblocks the v1.77.38 build chain that never published: #29 migration 058 idempotent for upgrade-in-place over a preexisting agent_health schema, #30 desktop package darwin headless stub so the cross-compile matrix succeeds for macOS amd64+arm64, #31 enrollment-paths cert issuance now subject to the same per-agent rate limit as re-cert (closes #22 H2/H3 gap), #32 macOS Distribution.xml embedded scripts wrapped in CDATA so productbuild's XML parser stops rejecting the JS '<' comparison, #33 capture tests skip cleanly when DXGI/GPU unavailable on CI runners. Server-side patcher fix from PR #27 (#26) is already live on production at version 1.78.0.",
  "platforms": [
    "windows",
    "linux",
    "darwin"
  ],
  "minAppVersion": "1.50.0",
  "signature": ""
}
